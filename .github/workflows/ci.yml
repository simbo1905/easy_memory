name: EasyMemory CI

on:
  push:
    branches: [ "main", "master" ]
    paths:
      - 'easy_memory.h'
      - 'Makefile'
      - 'tests/**'
      - '.github/workflows/ci.yml'
  pull_request:
    branches: [ "main", "master" ]

env:
  REGISTRY_IMAGE_BASE: ghcr.io/easymem/easy_memory

jobs:
  # Job 1: Build and test on primary x86_64 platforms
  build-and-test-x86_64:
    name: ${{ matrix.os }} | x86_64 | ${{ matrix.compiler }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        compiler: [gcc, clang]
        exclude:
          - os: windows-latest
            compiler: clang
          - os: macos-latest
            compiler: gcc

    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Linux Setup
      - name: Set up build environment (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y build-essential valgrind ${{ matrix.compiler }}

      # macOS Setup (uses pre-installed tools)
      - name: Set up build environment (macOS)
        if: runner.os == 'macOS'
        run: echo "Using pre-installed Clang on macOS."

      # Windows Setup (GCC via MSYS2)
      - name: Set up MSYS2 and GCC (Windows)
        if: runner.os == 'Windows'
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: mingw-w64-x86_64-toolchain make

      # Build & Test Steps
      - name: Build and Test with Makefile
        if: runner.os != 'Windows'
        run: make tests CC=${{ matrix.compiler }}
      
      - name: Verify CTZ Fallback Logic (Force Generic)
        if: runner.os == 'Linux'
        run: make test_fallback CC=${{ matrix.compiler }}

      - name: Build and Test with Makefile (Windows/GCC)
        if: runner.os == 'Windows'
        shell: msys2 {0}
        run: make tests CC=gcc

      # MSVC Smoke Test (Windows Only)
      - name: Build and Test each test file with MSVC
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          call "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\VC\Auxiliary\Build\vcvars64.bat"
          cd tests
          FOR %%f IN (*.c) DO (
            echo --- Compiling %%f with MSVC ---
            cl.exe /W4 /WX /DDEBUG /I.. /Fe:%%~nf.exe %%f
            echo --- Running %%~nf.exe ---
            call %%~nf.exe
          )

      # Valgrind & Codecov (Linux/GCC Only)
      - name: Run Valgrind Memory Check
        if: runner.os == 'Linux' && matrix.compiler == 'gcc'
        run: make valgrind CC=gcc

      - name: Build for Coverage
        if: runner.os == 'Linux' && matrix.compiler == 'gcc'
        run: |
          sudo apt-get update && sudo apt-get install -y lcov
          make coverage CC=gcc

      - name: Upload coverage to Codecov
        if: runner.os == 'Linux' && matrix.compiler == 'gcc'
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage.info
          token: ${{ secrets.CODECOV_TOKEN }}
          config_file: ./.github/codecov.yml
          fail_ci_if_error: true
          verbose: true

  # Job 2: Build and test for 32-bit architecture (i686)
  build-and-test-32bit:
    name: Ubuntu | x86_32 | GCC
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Set up 32-bit build environment
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-multilib gdb
      - name: Build and Test in 32-bit mode
        run: make tests CC="gcc -m32"

  # Job 3: Modern ARM64
  build-and-test-arm64-modern:
    name: Ubuntu | ARM64 (Modern) | GCC
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-qemu-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Test in ARM64 Container
        run: |
          docker run --rm --platform linux/arm64 \
            -v ${{ github.workspace }}:/workspace \
            -w /workspace \
            ${{ env.REGISTRY_IMAGE_BASE }}:arm64 \
            make tests_full CC='gcc' SAN_FLAGS=""

  # Job 4: Strict ARM64
  build-and-test-arm64-strict:
    name: Ubuntu | ARM64 (Strict Alignment) | GCC
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-qemu-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Test in Strict ARM64 Container
        run: |
          docker run --rm --platform linux/arm64 \
            -v ${{ github.workspace }}:/workspace \
            -w /workspace \
            --env UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 \
            ${{ env.REGISTRY_IMAGE_BASE }}:arm64 \
            make tests_full CC='gcc' SAN_FLAGS="-fsanitize=alignment,undefined"

  # Job 5: Big Endian (s390x)
  build-and-test-big-endian:
    name: Ubuntu | Big Endian (s390x) | GCC
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-qemu-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Test in s390x Container
        run: |
          docker run --rm --platform linux/s390x \
            -v ${{ github.workspace }}:/workspace \
            -w /workspace \
            ${{ env.REGISTRY_IMAGE_BASE }}:s390x \
            make tests_full CC=gcc SAN_FLAGS=""

  # Job 6: ARM32
  build-and-test-arm32:
    name: Ubuntu | ARM32 (armv7) | GCC
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-qemu-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Test in ARM32 Container
        run: |
          docker run --rm --platform linux/arm/v7 \
            -v ${{ github.workspace }}:/workspace \
            -w /workspace \
            ${{ env.REGISTRY_IMAGE_BASE }}:arm32 \
            make tests_full CC='gcc' SAN_FLAGS=""

  # Job 7: Test various C standards
  build-and-test-C-stds:
    name: Build & Test (${{ matrix.os }}, ${{ matrix.std }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        std: [c99, c11, c17, c2x]

    steps:
      - uses: actions/checkout@v3

      - name: Compile and Test
        run: |
          make tests STD_C=${{ matrix.std }}
  
  # Job 8: Test various optimization levels
  build-and-test-optimizations:
    name: ${{ matrix.os }} | ${{ matrix.compiler }} | ${{ matrix.opt }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        compiler: [gcc, clang]
        opt: [-O1, -O2, -O3, -Os]
    steps:
      - uses: actions/checkout@v4
      - name: Build and Test
        run: |
          if [ "${{ matrix.compiler }}" = "gcc" ] && [ "${{ matrix.opt }}" = "-Oz" ]; then
            make tests CC=gcc EXTRA_CFLAGS="-Os"
          else
            make tests CC=${{ matrix.compiler }} EXTRA_CFLAGS="${{ matrix.opt }}"
          fi

  # Job 9: MSVC with different architectures and optimizations
  build-and-test-msvc:
    name: Windows | MSVC | ${{ matrix.arch }} | ${{ matrix.opt }}
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        arch: [x64, x86]
        opt: [/O1, /O2, /Ox]
    
    steps:
      - uses: actions/checkout@v4
      - name: Build and Test (MSVC)
        shell: cmd
        run: |
          if "${{ matrix.arch }}"=="x86" (
            call "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\VC\Auxiliary\Build\vcvars32.bat"
          ) else (
            call "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\VC\Auxiliary\Build\vcvars64.bat"
          )
          
          cd tests
          FOR %%f IN (*.c) DO (
            echo --- Compiling %%f with MSVC [${{ matrix.arch }}, ${{ matrix.opt }}] ---
            cl.exe /W4 /WX /DDEBUG ${{ matrix.opt }} /I.. /Fe:%%~nf.exe %%f
            if errorlevel 1 exit /b 1
            echo --- Running %%~nf.exe ---
            %%~nf.exe
            if errorlevel 1 exit /b 1
          )